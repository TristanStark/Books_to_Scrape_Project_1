[project]
name = "project-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bs4>=0.0.2",
    "pytest>=9.0.2",
    "requests>=2.33.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
